#include <stdio.h>

/**
 * main - Print a simple message and exit
 *
 * Return: 0 - exit status zero if all ok
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle\n");
	return (0);
}
