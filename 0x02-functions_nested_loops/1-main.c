#include "main.h"

/**
 * main - prints alphabet in lowercase without the std library
 * users _putchar.c include file
 * Return: 0 - exit status zero if all ok
 */
int main(void)
{
	print_alphabet();
	return (0);
}

