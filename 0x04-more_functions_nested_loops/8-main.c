/*****************************************************************************/
#include "main.h"

/**
 * main - draw a square on screen
 * using _putchar()
 *
 * Return: 0 if all ok
 */
int main(void)
{
	print_square(2);
	print_square(10);
	print_square(0);
	return (0);
}

